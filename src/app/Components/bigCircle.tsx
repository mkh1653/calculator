import React from "react";
import styles from "../style.module.css";

const BigCircle = () => {
  return (
    <div
      className={`${styles.bigCircle} bg-sky-500 rounded-full absolute`}></div>
  );
};

export default BigCircle;
